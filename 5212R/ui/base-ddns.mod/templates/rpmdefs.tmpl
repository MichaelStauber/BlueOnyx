# configuration bits for mod_rpmize
# syntax:
# <begin [x]section>
# <end [x]section>
#
# x an be $ for a string or % for a hash.
# if a hash is being used, you can add sub-sections with
# <begin sub-section>
# <end sub-section>


<begin $DESCRIPTION>
%package [LABEL]
Group: System Environment/BlueQuartz
Summary: [LABEL] for [VENDOR]-[SERVICE].
[AUTOFILL]
[BUILDARCH]
[PROVIDES]

%description [LABEL]
The [VENDOR]-[SERVICE]-[LABEL] package contains the [LABEL]
information for [VENDOR]-[SERVICE].

<end $DESCRIPTION>

<begin %PREP>
<end %PREP>

<begin %SETUP>
<end %SETUP>

<begin %BUILD>
<end %BUILD>

<begin %INSTALL>
<end %INSTALL>

<begin %FILES>
<begin HEADER>
%files [LABEL]
%defattr(-,root,root)
<end HEADER>

<begin locale>
[AUTOFILL]

<end locale>

<begin glue>
%attr(0755,root,root) /usr/sausalito/sbin/ddns-client.pl

[AUTOFILL]
<end glue>

<begin ui>
[AUTOFILL]

<end ui>

<begin capstone>
%{RootDir}/capstone/%{Vendor}-%{Service}.cap
[AUTOFILL]

<end capstone>
<end %FILES>

<begin %POST-INSTALL>
<begin HEADER>
%post [LABEL]
<end HEADER>

<begin glue>
# glue post-install
# Rehash CCEd once YUM is done:
if [ -d /usr/sausalito/yumcce ];then
        touch /usr/sausalito/yumcce/rehash
fi

<end glue>
<begin ui>
# ui post-install
# this is somewhat nasty, but it always makes sure that there's a
# default .html file in case the browser doesn't specify a locale
if [ x"[DEFLOCALE]" != x ]; then
	for dir in `find [CCEWEB] -type d`; do 
		(cd $dir; 
		if [ x"`ls *.htm[l].[DEFLOCALE] 2> /dev/null`" != x ]; then
			for file in `ls *.htm[l].[DEFLOCALE]`; do
				base=`basename $file .[DEFLOCALE]`
				if [ ! -f $base ]; then
					ln -s $file $base
 				fi
			done
		fi)
	done
fi

<end ui>
<begin locale>
# locale post-install

<end locale>
<end %POST-INSTALL>

<begin %POST-UNINSTALL>
<begin HEADER>
%postun [LABEL]
<end HEADER>

<begin glue>
# glue post-uninstall
<end glue>
<begin ui>
# ui post-uninstall

<end ui>
<begin locale>
# locale post-uninstall

<end locale>
<end %POST-UNINSTALL>

<begin $CHANGELOG>

* Wed May 28 2025 Michael Stauber <mstauber@solarspeed.net> 1.2.1-2
- Small fix in glue/sbin/ddns-client.pl

* Tue May 27 2025 Michael Stauber <mstauber@solarspeed.net> 1.2.1-1
- Due to CSRF and other protection mechanism GUI no longer accepts
  foreign POST requests. Hence /ddns/ddnsapi and ddns-client.pl need
  to be switched to GET requests instead.
- Modified glue/sbin/ddns-client.pl to send GET request
- Modified ui/chorizo/web/Controllers/Ddnsapi.php to disable POST
  request handling, set BxPage to use setOutOfStyle() and trigger
  ACLs not via BaseController (needs AUTH), but via BX_SESSION vars.
  Not being 'systemAdministrator' or absence of sessionId triggers
  403 error.

* Mon Feb 19 2024 Michael Stauber <mstauber@solarspeed.net> 1.2.0-2
- Version number bump for Elmer release

* Sat Dec 02 2023 Michael Stauber <mstauber@solarspeed.net> 1.2.0-1
- Elmer'ized ui/chorizo/web/Controllers/Ddnsapi.php
- Elmer'ized ui/chorizo/web/Controllers/Ddnsconfig.php

* Mon Apr 03 2023 Michael Stauber <mstauber@solarspeed.net> 1.1.1-1
- Modified glue/sbin/ddns-client.pl to set timeout and ssl_opts

* Fri Nov 18 2022 Michael Stauber <mstauber@solarspeed.net> 1.1.0-1
- Release version for BlueOnyx 5211R

* Fri Sep 23 2022 Michael Stauber <mstauber@solarspeed.net> 1.0.3-1
- Modified glue/sbin/ddns-client.pl to use GET instead of POST
  requests. 

* Mon Sep 13 2021 Michael Stauber <mstauber@solarspeed.net> 1.0.2-1
- Added sorting to the display of domains.

* Sun Sep 12 2021 Michael Stauber <mstauber@solarspeed.net> 1.0.1-1
- API page now enforces HTTPS

* Sun Sep 12 2021 Michael Stauber <mstauber@solarspeed.net> 1.0.0-1
- Inital Build

<end $CHANGELOG>
