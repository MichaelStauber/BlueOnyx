# Temporary compatibility bridge for residual hardcoded image paths.
# Keep this narrow on purpose: only old Adminica image URLs are remapped.
AliasMatch ^/\.adm/images/(.*)$ /usr/sausalito/ui/web/.elm/images/$1
